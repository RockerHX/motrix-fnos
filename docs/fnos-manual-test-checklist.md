# 飞牛实机安装与基础功能测试清单

## 目的

沉淀 FPK 在飞牛 fnOS 上的安装、启动、停止、卸载和基础下载功能验证步骤，作为阶段 5 的验收记录模板。

## 当前状态

阶段 4 已完成 FPK 打包链路。当前进入阶段 5：基于真实飞牛设备验证安装、启动、Web UI、下载任务、停止、重启恢复和卸载闭环。

已知注意事项：

- `pnpm run build:fpk` 默认同时生成 x86 与 ARM 包。
- ARM 飞牛设备（例如 OES / A311D）不能安装 x86 包，安装时会提示“应用包不符合系统要求”。
- ARM 设备应安装 `packaging/fnos/dist/motrix.fnos_0.1.0_arm.fpk`。

## 测试前准备

| 项目 | 记录 |
| --- | --- |
| 测试日期 |  |
| 测试人员 |  |
| Git commit |  |
| fnOS 设备型号 |  |
| CPU 架构 | 例如 x86_64 / aarch64 |
| fnOS 版本 |  |
| FPK 文件 | 例如 `motrix.fnos_0.1.0_arm.fpk` |
| FPK 构建命令 |  |
| 下载保存目录 |  |

### 架构确认

在飞牛设备上确认 CPU 架构：

```bash
uname -m
```

对应关系：

| `uname -m` | 应安装的 FPK | 构建命令 |
| --- | --- | --- |
| `x86_64` | `motrix.fnos_0.1.0_x86.fpk` | `rtk pnpm run build:fpk` 或 `rtk pnpm run build:fpk:x64` |
| `aarch64` / `arm64` | `motrix.fnos_0.1.0_arm.fpk` | `rtk pnpm run build:fpk` 或 `rtk pnpm run build:fpk:arm64` |

## 构建验证

| 检查项 | 预期结果 | 结果 | 备注 |
| --- | --- | --- | --- |
| x86 包构建 | 生成 `packaging/fnos/dist/motrix.fnos_0.1.0_x86.fpk` |  | 仅 x86 设备安装 |
| ARM 包构建 | 生成 `packaging/fnos/dist/motrix.fnos_0.1.0_arm.fpk` |  | OES / A311D 使用 |
| 组装目录清理 | `packaging/fnos/app/data/` 不包含本地 SQLite/WAL/PID/日志残留 |  |  |
| 包内包含 server | `app/bin/motrix-fnos-server` 存在且可执行 |  |  |
| 包内包含 Aria2 | `app/bin/aria2-next` 存在且可执行 |  |  |
| 包内包含 Web UI | `ui/dist/index.html` 存在 |  |  |

## 安装验证

| 检查项 | 预期结果 | 结果 | 备注 |
| --- | --- | --- | --- |
| 上传 FPK | 应用中心可选择并识别 FPK |  |  |
| 安装 x86 包到 ARM 设备 | 应被拒绝，提示不符合系统要求 |  | 这是预期失败 |
| 安装匹配架构 FPK | 安装成功 |  | 若仍提示不符合系统要求，优先检查 manifest 是否含 `arch = x86_64`、`platform` 是否匹配、`os_min_version` 与 `config/privilege` |
| 应用信息 | 名称、版本、图标显示正常 |  |  |
| 安装后无自动异常退出 | 应用未出现安装后立即崩溃提示 |  |  |

## 启动、状态和停止验证

| 检查项 | 预期结果 | 结果 | 备注 |
| --- | --- | --- | --- |
| 启动应用 | 应用中心显示运行中 |  |  |
| 状态查询 | `cmd/status` 返回运行中、PID 和监听地址 |  |  |
| API ping | `/api/app/ping` 返回 `ok: true` |  |  |
| 停止应用 | 应用中心可停止服务 |  |  |
| 停止收口 | server 退出，PID 文件清理，Aria2 受管进程停止 |  |  |
| 重复停止 | 不报错，显示未运行或无需停止 |  |  |

可选 SSH 检查：

```bash
curl http://127.0.0.1:17080/api/app/ping
curl http://127.0.0.1:17080/api/app/info
```

## Web UI 验证

| 检查项 | 预期结果 | 结果 | 备注 |
| --- | --- | --- | --- |
| 打开应用入口 | Web UI 正常加载 |  |  |
| 任务列表首屏 | 空任务或历史任务正常显示 |  |  |
| SSE 连接 | 页面能接收 `tasks.snapshot`，任务状态自动刷新 |  |  |
| 退出态提示 | 服务停止时页面进入退出/不可操作状态 |  |  |
| 刷新页面 | 页面重新加载后可继续拉取任务列表 |  |  |

## 下载任务验证

建议准备两个小文件 URL：一个 HTTP，一个 HTTPS。

| 检查项 | 预期结果 | 结果 | 备注 |
| --- | --- | --- | --- |
| 新建 HTTP 任务 | 任务创建成功并开始下载 |  |  |
| 新建 HTTPS 任务 | 任务创建成功并开始下载 |  |  |
| 进度显示 | 进度、速度、大小、状态正常刷新 |  |  |
| 暂停任务 | 任务状态变为暂停 |  |  |
| 继续任务 | 任务继续下载 |  |  |
| 删除任务不删文件 | 任务从列表移除，文件保留 |  |  |
| 删除任务并删文件 | 任务从列表移除，文件删除 |  |  |
| 下载完成 | 文件完整落盘，状态为完成 |  |  |
| 错误 URL | 显示失败状态，日志可见错误原因 |  |  |

## 设置与日志验证

| 检查项 | 预期结果 | 结果 | 备注 |
| --- | --- | --- | --- |
| 读取设置 | 设置弹窗可正常打开并显示当前配置 |  |  |
| 保存设置 | 修改配置后保存成功 |  |  |
| 刷新后设置保持 | Web UI 刷新后配置仍存在 |  |  |
| 重启后设置保持 | 停止并启动应用后配置仍存在 |  |  |
| 查看诊断日志 | 能看到启动、Aria2、任务操作日志 |  |  |
| 清空诊断日志 | 清空后列表为空或只剩新日志 |  |  |
| 敏感信息 | 日志不暴露私密 URL query 或敏感配置 |  |  |

## Session 恢复验证

| 检查项 | 预期结果 | 结果 | 备注 |
| --- | --- | --- | --- |
| 创建大文件任务 | 下载中可观察进度 |  |  |
| 下载中停止应用 | 停止流程完成，session 保存 |  |  |
| 重启应用 | 未完成任务恢复到任务列表 |  |  |
| 默认状态 | 未完成任务默认暂停或处于可继续状态 |  |  |
| 手动继续 | 可断点续传，不从 0 重新开始 |  |  |
| 完成后重启 | 完成任务状态保持 |  |  |

## 卸载验证

| 检查项 | 预期结果 | 结果 | 备注 |
| --- | --- | --- | --- |
| 停止后卸载 | 卸载成功 |  |  |
| 运行中卸载 | fnOS 能先停止或拒绝并给出明确提示 |  |  |
| 端口释放 | `17080` 不再监听 |  |  |
| 进程清理 | `motrix-fnos-server` 和受管 `aria2-next` 不再运行 |  |  |
| 残留检查 | 无明显 PID、临时运行态残留 |  |  |

## 问题记录

| 编号 | 场景 | 现象 | 日志/截图 | 判断 | 后续处理 |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |

## 阶段 5 验收结论

- [ ] 匹配架构的 FPK 可安装。
- [ ] 应用可启动、停止、查询状态。
- [ ] Web UI 可打开并调用后端 API。
- [ ] HTTP/HTTPS 下载、暂停、继续、删除可用。
- [ ] 设置、日志和 session 恢复可用。
- [ ] 卸载无明显残留。

## 与其他文档关系

- 架构和范围见 `docs/architecture.md` 与 `docs/fnos-fpk-architecture.md`。
- API 与事件流见 `docs/api-contract.md`。
- 打包和安装来源见 `docs/fpk-packaging.md`。
