# Changelog

## 1.8.1 - 2026-07-24

### 新增

- 任务快照增加递增版本号，支持丢帧后完整重同步，提升同步可靠性。

### 改进

- 服务启动流程增加就绪状态检查，避免假成功导致的异常。
- 限制管理 API、上传和 WebSocket 请求体大小及超时，防止资源滥用。
- 对账 RPC 请求结果，避免重复创建任务，支持人工处理无法唯一匹配情况。
- 统一 Aria2 RPC 客户端管理，统一超时和错误处理，避免误判重试。
- 任务操作新增持久化记录，启动时自动恢复未完成操作，避免状态不一致。
- 统一任务操作互斥，防止同一任务多操作冲突，冲突请求返回 409。

### 修复

- 修复任务状态因旧请求乱序导致的展示异常，确保任务状态显示稳定。

## 1.8.0 - 2026-07-22

### 新增

- 新增 JSON-RPC 配置引导窗口及端口提示，优化用户配置体验。

### 修复

- 统一 FN Connect 应用入口为 motrix，确保入口身份一致。
- 修正残留绿色主题色，完成蓝色主题色迁移。

## 1.7.9 - 2026-07-21

### 新增

- 新增回收站任务恢复功能，支持任务恢复操作及状态接入，并持久化恢复上下文。

## 1.7.8 - 2026-07-19

### 新增

- 支持双击任务切换暂停状态，提升任务操作便捷性。
- 增加任务来源标识，方便区分任务来源。

### 改进

- 使用网络拓扑图标表示种子任务，提升界面信息表达。
- 优化小窗口新建任务操作区，提升用户操作体验。

### 修复

- 等待 Aria2 暂停状态稳定，避免状态不一致问题。
- 兼容删除历史 BT 任务目录，防止删除异常。
- 移除 BT 任务目录文件后缀，规范目录结构。

## 1.7.7 - 2026-07-18

### 改进

- 试点 UnoCSS 原子布局及组件样式外置，明确相关规则和断点说明。
- 外置诊断、关于帮助、设置认证、任务、布局及通用界面组件样式。

### 修复

- 修复无可发布事实时回退发布日志的问题。

## 1.7.6 - 2026-07-18

### 改进

- 优化设置弹窗层级与布局，统一卡片层级，改善桌面和移动端排版体验。
- 统一高清图标资源和预检尺寸，采用256×256 PNG格式，确保跨平台一致性。

### 修复

- 修复安全弹窗宽度，桌面端最大520px，移动端自适应，提升界面一致性。

## 1.7.5 - 2026-07-16

### 新增

- 建立交互动效基础规则，规范界面动效实现。

### 改进

- 优化任务进度条动画，提升流畅度并统一动效时长。
- 新增浮动按钮显隐过渡，提升移动端浮动按钮交互体验。
- 新增顶栏分类标题和主内容分类切换的过渡动画，界面切换更自然。
- 完善顶栏、侧栏及任务操作按钮的交互反馈，提升操作响应体验。

### 修复

- 修复发布日志生成异常，解决 GitHub 限流和 JSON 截断问题。
- 支持 beta 测试版，移除自动测试版本的迭代限制。

## 1.7.4 - 2026-07-15

### 新增

- 新增应用启动加载界面，包含动画和状态提示，提升用户启动体验。
- 新增 GitHub Actions 工作流用于自动清理旧的 Action 运行记录和 Release 标签，仅保留最近版本，支持预览和确认删除。
- 新增启动层控制器管理鉴权确认显示时长和淡出动画。

### 改进

- 将主窗口组件改为异步加载，提升启动性能和用户体验。
- 增强发布相关 CI 工作流，增加旧版本清理和日志审稿流程。
- 发布补丁生成时自动忽略锁文件、构建产物和生成目录，限制补丁最大字节数为 160KB，确保发布内容规范。

### 修复

- 优化首次鉴权启动流程，提前显示鉴权加载反馈，减少黑屏和状态闪烁。
- 修复关于页面显示未分类版本日志问题。

### 文档

- 新增开发脚本与命令说明文档，详细介绍命令用途、工作流、清理与版本管理等内容。

## 1.7.3 - 2026-07-14

- 注入 JSON-RPC 回环监听配置。
- 增加 Web 管理安全设置。
- 实现管理初始化与登录访问门。
- 建立前端鉴权状态与请求管线。
- 增加本地 Web 鉴权恢复命令。
- 提供 Web 管理鉴权接口。
- 增加管理登录失败限速。
- 实现服务端 Session 与 CSRF 校验。
- 建立 Web 鉴权持久化与密码模型。
- 支持管理与 RPC 双监听器。
- 增加 JSON-RPC 回环监听配置。

### 改进
- 记录访问入口不兼容变更。
- 统一保护管理 API 与 SSE。
- 隔离管理与 JSON-RPC 路由。

### 修复
- 清理管理端旧 JSON-RPC 路由约定。
- 阻止 RPC 端口进入 fnOS 端口映射。
- 迁移 JSON-RPC Token 专用管理界面。
- 统一处理会话失效与 SSE 重连。
- 隔离 JSON-RPC Token 读写。

### 文档
- 定义双监听器打包与实机验收。
- 定义前端鉴权流程与视觉规范。
- 定义管理鉴权与 RPC 隔离契约。
- 明确管理面与 RPC 双监听器架构。

## 1.7.2 - 2026-07-14

- 同步 FPK 入口缓存版本。
- 恢复端口入口并修复 Web UI 404 问题。
- 稳定共享指标类型导入功能。
- 对齐统一网关入口与转发路径。
- 支持配置桌面访问用户范围。
- 恢复统一网关 Web UI 路由。
- 避免统一网关入口回退到服务端口。

### 文档
- 记录统一网关最小包验证计划。 

### 改进
- 统一使用 Tabler 图标。

## 1.7.1 - 2026-07-13

- 补充任务异步状态中文注释。
- 补充网关与授权目录同步中文注释。
- 补充目录所有权与退出顺序中文注释。
- 补充批量创建与会话恢复中文注释。
- 补充启动对账与入口边界中文注释。
- 补充关键状态与发布约束中文注释。
- 明确测试代码独立文件约束。

### 改进
- 移除业务模块测试专用导入。
- 明确 Aria2 资产维护入口。
- 补充构建与发布脚本回归测试。

## 1.7.0 - 2026-07-13

- 移除 UI 偏好预留接口约束及相关存储。
- 清理前端和后端无效设置字段。
- 抽离新建任务相关功能，提升代码结构清晰度。
- 拆分任务相关弹窗，优化用户交互体验。
- 精简长期架构约束及产品与构建文档职责。

### 文档
- 补充运行时与网关安全说明。
- 补充任务清理安全说明。
- 补充进程身份校验说明。
- 说明品牌 SVG 源文件用途。
- 精简 JSON-RPC 说明。

## 1.6.5 - 2026-07-13

- 固定 pnpm 版本以规避自更新故障。
- 防止 PID 复用导致进程误判。
- 接入飞牛统一网关访问控制。
- 防止符号链接导致 BT 任务目录误删。

### 文档
- 删除已完成的任务拆分文档。
- 完成核心任务模块拆分收口文档。

### 改进
- 收口主窗口页面编排。
- 抽离顶部任务操作状态。
- 抽离批量任务操作。
- 收口任务服务入口。
- 抽离磁链解析与文件确认流程。
- 抽离 URL 与种子任务创建流程。
- 抽离任务控制流程。
- 抽离任务删除与回收站流程。
- 抽离任务查询与持久化同步。
- 整理任务服务测试模块。

## 1.6.4 - 2026-07-12

- 添加任务列表分页功能。
- 新增全部任务导航。

### 改进
- 优化桌面任务列表样式。
- 调整前端分包警告阈值。
- 移除验证中的 fpk 准备步骤。
- 使用快速验证进行发布。

### 修复
- 消除组件属性类型警告。
- 收紧任务进度指标间距。
- 避免操作加载图标重叠。
- 调整全局通知位置。

## 1.6.3 - 2026-07-12

- 支持一键清空回收站功能。
- 合并单个与批量链接创建功能。

### 修复
- 稳定下载进度指标布局。

### 文档
- 精简 Stitch 设计协作文档。
- 固化 Stitch MCP 单页验证流程。
- 切换到新版 UI 设计文档。
- 建立 UI 重设计需求与设计输入。

## 1.6.2 - 2026-07-12

- 新增通用信息区块组件。
- 新增通用状态摘要组件。
- 新增通用弹窗组件。

### 改进
- 将辅助功能入口迁移到桌面侧栏。
- 复用通用信息区块组件。
- 复用状态摘要卡片组件。
- 复用移动任务卡片展示组件。
- 抽取桌面任务卡片共享展示。
- 使用 Naive UI 进度条重构任务进度。
- 使用 Naive UI 输入框承载日志手动复制。
- 使用数据表格重构文件确认列表。
- 复用任务操作确认弹窗。
- 抽取批量删除确认弹窗。
- 复用通用弹窗壳。
- 使用 Naive UI 上传组件选择种子文件。
- 使用 Naive UI 按钮重构空状态操作。
- 使用 Naive UI 按钮重构引擎状态面板。
- 使用 Naive UI 按钮重构任务图标操作。

### 修复
- 修复任务空状态图标与居中布局。
- 清理 P2 旧样式命名残留。
- 压缩下载 UI 桌面视觉密度。
- 抑制 Linux server 构建链路链接器警告。

## 1.6.1 - 2026-07-10

- 新增本地 SVG 图标组件。

### 修复
- 补充下载 UI 视觉回归测试。
- 优化任务卡片信息层级。
- 重构桌面任务卡片紧凑布局。
- 统一下载 UI 顶部与内容对齐。
- 精修下载 UI 侧栏视觉密度。
- 隐藏桌面端重复新建浮动按钮。
- 优化任务操作图标胶囊。
- 替换下载 UI 工具栏字符图标。
- 降低下载 UI 桌面显示密度。

### 改进
- 优化 CI 操作缓存保留策略。

## 1.6.0 - 2026-07-10

- 接入可见任务批量删除确认功能。
- 补齐顶部工具栏禁用提示。
- 接入可见任务批量暂停和继续功能。
- 接入顶部新建与刷新操作。
- 增加下载顶部工具栏操作入口。
- 启用桌面任务卡片的图标操作区。
- 支持任务操作展示模式透传。
- 增加任务操作图标的胶囊模式。
- 切换桌面任务列表为卡片视图。
- 新增桌面任务卡片列表和组件。
- 抽出任务进度条组件。
- 重构下载 UI 的桌面顶栏和侧栏样式。
- 调整下载 UI 的桌面外壳布局。
- 补充下载 UI 外壳的语义 token。

### 改进
- 补充下载 UI 中英文布局回归测试。
- 完善完成任务进度条的视觉效果。
- 稳定任务卡片长文本与错误布局。
- 统一任务数字信息的等宽显示。

### 修复
- 修复任务卡片长文本与错误布局问题。

## 1.5.8 - 2026-07-10

- 降低 Aria2 就绪检查日志噪音。

### 文档
- 更新诊断日志接口说明。

### 新增
- 升级诊断日志查看器。
- 重构后端诊断日志体系。

## 1.5.7 - 2026-07-10

- 修复种子任务完成后继续做种的问题。
- 修复磁链确认下载的路径预览和重复种子的问题。
- 修复磁链 metadata 同步污染保存目录的问题。
- 修复卸载向导开关默认值异常的问题。
- 修复磁链确认下载使用错误保存目录的问题。

### 改进
- 更新 fpk-packaging.md 文档。

## 1.5.6 - 2026-07-10

- 启动时标记失效的磁链解析任务。
- 删除解析中磁链任务时清理 metadata 目录。

### 新增
- 展示磁链解析中和待确认状态。
- 启动时清理磁链 metadata 残留。
- 重构磁链转种子下载主流程。
- 持久化磁链 metadata 关联信息。

### 文档
- 明确磁链 metadata 临时解析流程。

## 1.5.5 - 2026-07-10

### 改进

- 调整磁链文件确认与真实任务创建流程。
- 改进 BT 与磁链启动参数。
- 更新 fnOS 图标。

## 1.5.4 - 2026-07-09

- 添加新的 logo 图标。

### 改进
- 更新 logo 图标。

## 1.5.3 - 2026-07-09

- 更新 manifest.template 文件。

### 文档
- 完成收口阶段 12 开发计划文档。

### 修复
- 修复与 Aria2 文件索引字符串的兼容性问题。

## 1.5.2 - 2026-07-08

- 保存磁链解析种子元数据。
- 支持磁链解析后确认文件。

### 修复
- 跟随磁力链接元数据下载任务。
- 保留 Aria2 原生种子元数据。
- 避免种子任务生成重复种子文件。

## 1.5.1 - 2026-07-08

- 补充 BT 下载配置和诊断字段。
- 使用专属目录管理种子任务文件。

### 文档
- 同步种子任务目录行为。
- 补充批量任务创建架构约束。
- 同步新建任务能力说明。

## 1.5.0 - 2026-07-08

- 完善新建下载任务弹窗。
- 接入新建任务高级设置。
- 支持种子文件上传创建任务。
- 支持批量 URL 创建任务。
- 支持磁力链接和添加后暂停。
- 扩展新建任务契约和任务分类字段。

### 文档
- 标记阶段 12 新建任务能力完成。
- 细化阶段 12 新建任务实施清单。

### 改进
- 缩短发布产物缓存保留期。

## 1.4.2 - 2026-07-07

- 改为单工作流自动发版。
- 优化 Actions 缓存清理策略。
- 控制 Actions 缓存占用。
- 自动清理 Rust 增量缓存。
- 限制自动合并仅监听发版分支。
- 使用 GitHub Models 生成发版日志。
- 自动合并已验证的发版 PR。
- 减少发版流程重复验证。
- 增加 GitHub 自动 PR 发版流程。

### 修复
- 修复 JSON-RPC 测试临时库冲突。
- 修复任务创建弹窗测试告警。

### 文档
- 更新自动发版流程说明。

## 1.4.1 - 2026-07-07

### 新增

- 增加本地快速发版命令 `pnpm run release:prepare <x.y.z>`，自动串联版本同步、本地验证、release commit 与 tag 生成。

### 改进

- 发版脚本优先复用 `CHANGELOG.md` 中已填写的目标版本条目，降低发版前的重复操作成本。
- 未预先填写 CHANGELOG 时，按 commit log 生成确定性草稿，避免依赖额外 AI/API 环境配置。
- 在自动提交前检查工作区状态、目标 tag、验证结果和非预期文件改动，降低误发版风险。

## 1.4.0 - 2026-07-07

### 新增

- 增加项目版本同步命令，可通过 `pnpm run version:set <x.y.z>` 一次性更新前端、后端、FPK manifest 与 UI 入口缓存版本。
- 增加项目版本一致性检查命令 `pnpm run version:check`，用于在本地和 CI 中阻断版本号漏改或不一致的发布。

### 改进

- 将版本一致性检查纳入 `pnpm run verify`，使日常验证、提交前快速验证和 Release 门禁使用同一套版本校验逻辑。
- 优化 Release FPK 工作流，在构建和发布 FPK 产物前先执行完整验证，避免出现 Release 成功但 Verify 失败的发版状态。
- Release 版本解析改为复用统一版本检查脚本，减少 workflow 中重复的 shell 版本校验逻辑。

## 1.3.3 - 2026-07-06

### 新增

- 增加 FPK 卸载向导，用户可选择是否同时删除 Motrix 应用数据。

### 修复

- 修复卸载回调默认清理 `TRIM_PKGVAR` 导致升级后任务、设置和 JSON-RPC 密钥丢失的问题。
- 卸载默认保留应用数据，仅在用户明确开启删除选项时清理 Motrix 私有数据，且不删除用户下载文件。

## 1.3.2 - 2026-07-06

### 修复

- 恢复 `install_*`、`upgrade_*` 与 `config_init` 空壳生命周期脚本为纯空操作，避免安装阶段因写入运行日志或依赖运行期目录而失败。
- 保持 `config_callback` 的授权目录同步逻辑不变，继续由其承担真实配置回调职责。

## 1.3.1 - 2026-07-06

### 新增

- 增加关于页，展示应用信息、维护者、项目链接、Release 链接、版本检测和手动更新说明。
- 关于页展示内置更新历史，便于在应用内查看最近版本变化。

### 改进

- 扩展应用信息接口，补充维护者、仓库地址、Release 地址、目标架构和更新方式。
- 增加后端版本检测接口，检测失败时保留手动查看 Release 页的回退提示。
- FPK 打包改为基于 staging 目录的生成式组装流程，并增加 `fnpack` 构建前校验与产物检查。
- Linux server 构建统一通过 `cargo zigbuild` 固定 glibc baseline，提高 x86 / ARM 包在 fnOS 上的兼容性。

### 修复

- 修复空壳生命周期脚本把说明写入安装错误日志，导致应用中心误判安装失败的问题。

## 1.2.1 - 2026-07-05

### 新增

- 增加关于页，展示应用信息、维护者、项目链接、Release 链接、版本检测和手动更新说明。
- 关于页展示内置更新历史，便于在应用内查看最近版本变化。

### 改进

- 扩展应用信息接口，补充维护者、仓库地址、Release 地址、目标架构和更新方式。
- 增加后端版本检测接口，检测失败时保留手动查看 Release 页的回退提示。

## 1.2.0 - 2026-07-05

### 新增

- 完成手机端 UI 适配主线开发，覆盖移动端外壳布局、任务卡片、空态、创建任务、设置、帮助、诊断与日志弹窗。

### 改进

- 补齐移动端导航、状态同步、按钮触控区域、横向溢出控制与中英文长文案适配。
- 升级 GitHub Actions 关键 action 主版本，消除 Node.js 20 运行时弃用警告。

## 1.1.0 - 2026-07-05

### 新增

- 增加 JSON-RPC 密钥配置与校验，支持公网安全添加下载任务。
- 设置页新增 JSON-RPC 密钥输入、显隐切换和随机生成功能。

### 改进

- `aria2.getVersion` 保持匿名可用，便于外网连通性测试。
- `system.multicall` 中的 `aria2.addUri` 子调用也会校验密钥。

## 1.0.0 - 2026-07-05

### 新增

- 增加 Aria2 JSON-RPC 兼容入口，支持解析站直接发送任务到 NAS。
- 透传常用 Aria2 加速参数，提升 NAS 端下载速度表现。

### 修复

- 修正任务创建链路在不同入口下的参数兼容性。

## 0.2.1 - 2026-07-05

### 修复

- 优化任务进度动画性能，移除每个任务行持续运行的 `requestAnimationFrame` 循环，改为 CSS transition。
- 保留同一任务进度不回退保护，降低多任务下载时的 UI 卡顿风险。

## 0.2.0 - 2026-07-05

### 新增

- Web UI 增加应用内国际化，支持简体中文与 English 手动切换。
- 设置页新增界面语言选项，并通过现有设置接口持久化。

### 改进

- 前端可控文案统一接入轻量 i18n 字典，Naive UI locale 随语言切换。
- 继续保持 fnOS 平台外壳中文，不改变应用中心、桌面入口和安装向导文案边界。

### 修复

- 统一前端入口缓存参数，切换版本后可重新加载到最新 UI。
- 收口阶段 8 的数据目录生命周期与前端缓存策略说明。
